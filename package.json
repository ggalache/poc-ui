{
  "name": "poc-ui",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "react": "^16.8.1",
    "react-dom": "^16.8.1",
    "react-scripts": "2.1.3",
    "npm": "^6.4.1",
    "npm-run-all": "^4.1.3",
    "mock-json-server": "^1.0.0"
  },
  "devDependencies": {
    "mock-json-server": "^1.0.0"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test",
    "eject": "react-scripts eject",
    "mock-server": "node mock/build-data && mock-json-server data.json --port 3001",
    "offline": "npm-run-all -p start mock-server"
  },
  "eslintConfig": {
    "extends": "react-app"
  },
  "browserslist": [
    ">0.2%",
    "not dead",
    "not ie <= 11",
    "not op_mini all"
  ]
}
